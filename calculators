#include<stdio.h>//BMI(body mass index) calculator
void main()
{
    int w,h,BMI;
    printf("enter your height:");
    scanf("%d",&w);
    printf("enter your weight:");
    scanf("%d",&h);
    BMI=h*h/w;
    printf("BMI is %d",BMI);
}
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//simple interest calculator
#include<stdio.h>
void main()
{
    int p,n,r,simple_interest;
    printf("enter the principle value:");
    scanf("%d",&p);
    printf("enter the number of years:");
    scanf("%d",&n);
    printf("enter the rate of interest:");
    scanf("%d",&r);
    simple_interest=p*n*r/100;
    printf("simple interest is %d",simple_interest);
}

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 /*kalki wrote a book ponniyin selvan and published the first edition with 50,525 copies .since the book was popular among the peoples 
he decided to publish the second edition with 40,399.calculate the total number of copies pubished */
#include<stdio.h>
void main()
{
    int first_edition=50525,second_edition=40399;
    int sum;
    sum=first_edition+second_edition;
    printf("the total number of books published is %d",sum);
    
}

-------------------------------------------------------------------------------------------------------------------------------------------
//compound interest calculator
#include<stdio.h>
#include<math.h>
void main()
{
    int n;
    double p,r,t,compound_interest;
    printf("enter the principle value:");
    scanf("%lf",&p);
    printf("enter the rate of interest:");
    scanf("%lf",&r);
    printf("enter the time:");
    scanf("%lf",&t);
    printf("enter the number of years:");
    scanf("%d",&n);
    compound_interest=p*pow((1+r)/(100*n),n*t);
    printf("the compound interest is %2lf",&compound_interest);
    
    
}








